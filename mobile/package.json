{
  "name": "open-historia-mobile",
  "private": true,
  "version": "1.0.0",
  "description": "Android shell for Open Historia. It packages the WEB build, so the app connects to a community content node exactly like openhistoria.com — no server on the phone, no Termux.",
  "scripts": {
    "www": "node ./scripts/stage-www.mjs",
    "sync": "cap sync android",
    "open": "cap open android",
    "apk": "npm run www && cap sync android && cd android && gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.0.0"
  }
}
